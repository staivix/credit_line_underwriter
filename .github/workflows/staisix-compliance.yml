name: STAISIX Continuous AI Compliance Gate

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  staisix-compliance-scan:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read

    steps:
      - name: Checkout Codebase Repository
        uses: actions/checkout@v5

      - name: Setup Node.js Runtime
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      # 🔐 SECURE MULTI-REPOS ACCESS: Dynamically pulls your private engine using your encrypted secret token
      - name: Install STAISIX Enterprise CLI Tool
        run: npm install -g "https://${{ secrets.STAISIX_GITHUB_TOKEN }}@://github.com"

      - name: Run Live Compliance Verification Engine
        env:
          AIRTABLE_PAT: ${{ secrets.STAISIX_AIRTABLE_PAT }}
        run: staisix scan
